import React from 'react';
// Lightweight header proxy: imports the original page file so bundlers see the module
// No runtime behavior change — returns null and is not used by existing code.
import '../ProjectPage.jsx';

export default function ProjectHeader() {
  return null;
}
