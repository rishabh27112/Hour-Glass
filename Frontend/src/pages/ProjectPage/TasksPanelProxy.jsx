// Tasks panel placeholder file to start modularization.
// It imports the original ProjectPage.js to avoid changing runtime features.
import '../ProjectPage.jsx';

export const tasksPanelLoaded = true;
export default tasksPanelLoaded;
