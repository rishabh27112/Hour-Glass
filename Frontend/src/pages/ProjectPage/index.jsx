// Re-export the original ProjectPage component so other modules can import from './ProjectPage'
// This file intentionally references the original flat file to avoid changing runtime behavior.
export { default } from '../ProjectPage.jsx';
