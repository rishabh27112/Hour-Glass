// This module exists to host the EditMembers component in the future.
// For now it references the original page module so it's safe and non-breaking.
import '../ProjectPage.jsx';

export const EditMembersProxy = () => null;
export default EditMembersProxy;
