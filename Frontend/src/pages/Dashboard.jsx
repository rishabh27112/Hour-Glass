import ManagerDashboard from './ManagerDashboard';

// Re-export ManagerDashboard as the public `Dashboard` component.
// This keeps the existing file `ManagerDashboard.jsx` untouched while
// satisfying imports of `Dashboard` from `App.jsx`.
export default ManagerDashboard;
